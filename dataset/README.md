# Dataset Folder

This folder stores the face images captured by `face_dataset.py`.

Each image is saved in the format:
User.<ID>.<sample_number>.jpg

These images are used by `face_training.py` to train the LBPH face recognition model.

# Note:  
The image files are not included in the repository.